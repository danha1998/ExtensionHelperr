import XCTest
@testable import ExtensionHelperr

final class ExtensionHelperrTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExtensionHelperr().text, "Hello, World!")
    }
}
